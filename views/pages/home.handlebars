{{> header}}

<div>
    {{#each posts}}
        <div>
            <h2>{{title}}</h2>
            <p>{{content}}</p>
            <p>Created by {{user.username}} on {{created_at}}</p>
        </div>
    {{/each}}
</div>
